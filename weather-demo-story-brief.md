# 天气数仓问数购物车｜Demo 故事说明

> 2026-09-26 · 公开演示稿。故事方向已按用户“按照建议执行”实施；数值、问句识别和结果均为合成演示，不代表真实天气取数能力。

## 基本信息

- 受众与操作者：希望用天气事实做比较分析、但不熟悉天气表结构的业务人员。
- 一句话价值：用一个业务问题选定城市、时间与指标，只拿与本次问题有关的天气数据。
- 假设：公开 Demo 只展示五座样例城市和两版样例预报；它们不是正式系统的城市白名单或真实批次。

## 具体故事与结束产物

业务人员提出：“比较武汉和成都 2025 年 8 月每天的平均气温和降水，与常年相比有什么不同？”演示中的问句规则推荐历史实况、两座城市、一个日期范围、两个指标及同日常年基线。业务人员可在购物车中增删选项，生成只包含所选范围的合成趋势、城市汇总，以及一份给自己 AI 的只读取数需求说明。该说明没有执行入口和口令。

| 阶段 | 输入与来源 | 用户选择 | 输出与下一位使用者 | 就绪程度 |
| --- | --- | --- | --- | --- |
| 问题 | 业务人员的自然语言 | 选择示例问题或自行输入 | 可检查的查询条件 | 规则模拟；未接模型 |
| 购物车 | 公开演示的城市和字段清单 | 城市、目标日期、指标、数据类型 | 有边界的取数计划 | 浏览器内交互 |
| 结果 | 确定性合成天气值 | 切换已选图表指标 | 趋势和城市汇总 | 合成数据，非真实查询 |
| 交接 | 购物车条件 | 复制说明 | 给其他 AI 的取数需求 | 仅文本；真实接口待确认 |

## 操作情节与分支

| 场景 | 用户看到与操作 | 系统变化 | 可验收点 |
| --- | --- | --- | --- |
| 历史对比 | 问“武汉和成都 8 月平均气温、降水，与常年相比” | 推荐历史实况、日期、指标、可用基线 | 购物车可编辑；结果只含两城与所选日期 |
| 预报版本 | 切换到预报快照并选制作日 | 目标日期限制在该批次七天内 | 制作日与被预报日期分别显示；不混批 |
| 缺失基线 | 问“最高温比常年高多少” | 要求改成日均温对常年，或只看最高温 | 不生成不存在的最高温常年偏差 |
| 越界日期 | 选择反向或过长日期范围 | 阻止生成并说明原因 | 不展示伪结果 |

## 关键关系和边界

- 城市是维度，目标日期是必选过滤条件，气象指标是待取字段；这些可以在同一购物车展示，但语义不同。
- 历史实况、常年基线、预报快照分别对应不同事实类型。预报还必须锁定一个制作批次。
- `weather_daily.tmean` 可与 `climate_normal.tmean_n` 对照；`weather_daily.precipitation` 可与 `climate_normal.precip_n` 对照。`tmax` 没有相应常年基线，不能编造。
- 本轮演示结束于合成图表和取数需求说明。真正的天气只读入口、权限、数据新鲜度与模型接入不在本轮实施范围；天气数仓独立于数仓前台。
- 浏览器刷新即可恢复初始演示。缺少城市、指标或有效日期时，以明确错误结束，不生成结果。

## 开发与视觉交接

页面视觉中心是“城市 × 日期 × 指标”的购物车，旁边是精简趋势与城市汇总；悬浮“AI 问天气”提供三条可复现的示例问句。所有图表标记“合成数据”，所有取数说明标记“非可执行命令”。桌面与手机应能完成提问、选择、生成、切换图表、复制说明与重置。
